## 2026-06-27

- 实现完整的博客扩展系统：
  - 新建 `expand/` 根目录，含 `expand/config.json`（总控，按扩展 ID 启用/禁用）
  - 新建 `expand/expand-test/` 示例扩展，包含 `config.json`、`expand.css`、`expand.html`
  - 新建 `main/js/extensions.js` 扩展加载器：
    - 读取 `expand/config.json` → 遍历已启用扩展 → 读取各自 `config.json`
    - 动态注入 CSS（`<link>`）和 JS（`<script>`）
    - 注册扩展页面到 `window.__EXTENSIONS.pages`，路由前缀 `/ext/`
    - 自动往浮动导航栏添加扩展链接，往 sidebar 添加扩展页条目
    - 监听 hashchange，拦截 `/ext/` 路由，fetch 并渲染扩展 HTML
    - 更新激活导航状态
  - 修改 `page.js`：跳过 `/ext/` 前缀路由，交给 extensions.js 处理
  - 所有 4 个 HTML 页面引入 `extensions.js`
- 将 expand-test/expand.html 重写为完整的扩展系统开发者指南：
  - 快速开始（5 分钟上手）、完整目录结构图
  - 详细的 JSON Schema 文档（总控 config 和扩展 config，含字段说明）
  - 外观扩展开发指南（CSS 变量覆盖、代码示例）
  - 页面扩展开发指南（HTML 规范、预定义 CSS 类表格）
  - 脚本扩展开发指南（IIFE 模板、window.__EXTENSIONS 全局对象参考）
  - 图标列表、4 个完整扩展示例
  - AI/VibeCoding 开发规范（结构化指令，可供 AI 解析执行）
  - 常见问题排查（4 类问题 + 解决方案）、进阶技巧（7 条）
- 实现「瞬间」(Moments) 功能：
  - 新建 `moment/config.json`：总控 enabled + moments 数组，每个瞬间最多 100 字 + 10 张图
  - 新建 `main/css/moments.css`：横向滚动卡片样式，图片自适配网格（1-10 张不同布局），MD3 风格
  - 新建 `main/js/moments.js`：fetch moment/config.json → 渲染卡片，支持本地路径和远程 URL 图片，图片加载失败自动 fallback，文字超 100 字自动截断加省略号，按日期倒序排列
  - 在首页 topic cards 下方插入 moments section，默认隐藏，有数据时自动显示
  - 图片存放于 `moment/images/`，配置中使用相对路径或完整 URL
- 新增瞬间独立页面 `main/moment.html`：垂直时间线，每张卡片显示日期+文字+图片网格，空状态提示，与首页预览共享 `moment/config.json` 数据源
  - 所有 5 个 HTML 页面（index/page/moment/link/about）的浮动导航栏均已添加"瞬间"入口
  - `transitions.js` PAGE_ORDER 更新为包含 moment.html，页面滑动方向自动感知
- 修复瞬间图片不显示：去掉 onerror 复杂引号嵌套改用事件委托、去掉 object-fit cover/max-height 改用自然比例
- 去掉瞬间卡片 stagger-item 类防止 opacity:0 隐藏，CSS 强制 opacity:1
- 新建图片查看器 `main/img.html`：全屏黑色背景，鼠标滚轮缩放(0.1x-5x)、拖拽平移、+/- 按钮、Escape/点击背景关闭、3s 自动隐藏控件
- 新建 `main/js/lightbox.js`：事件委托监听所有页面图片点击，自动解析路径并存 sessionStorage 来源页，关闭时正确返回
- 所有 6 个 HTML 页面均引入 lightbox.js
