## 2026-06-16

- 为所有页面添加灵动的页面过渡动画：
  - 新建 `css/transitions.css`：页面退出动画（淡出+上移+缩小）、页面进入动画（淡入+上滑）、导航栏和 FAB 独立动画、内容交错浮现（stagger reveal）、`prefers-reduced-motion` 适配
  - 新建 `js/transitions.js`：拦截导航链接点击 → 退出动画 → 导航跳转；页面加载时进入动画 + 内容交错展示；MutationObserver 自动检测动态渲染的卡片并应用动画；同页面链接跳过动画直接平滑滚动；bfcache 兼容
  - 所有 4 个 HTML 页面（index/page/link/about）均已引入 CSS 和 JS
- 将页面级过渡改为横向滑块滑动动画：
  - 退场：内容向左滑出（translateX -40px） + 淡出
  - 前进：新页从右侧滑入（translateX 50px → 0）+ 淡入
  - 后退：新页从左侧滑入（translateX -50px → 0）+ 淡入
  - 通过 sessionStorage 传递前页名，根据 PAGE_ORDER 判断滑动方向
  - 内容交错浮现（stagger）动画保持不变
- 新增导航栏滑块动画指示器（nav-slider）：
  - 在 `.floating-nav__links` 中添加 `.nav-slider` 绝对定位的 pill 元素
  - 页面切换时，滑块从上一个激活按钮的位置平滑滑动到当前激活按钮的位置
  - 通过 sessionStorage 存储前页激活链接索引，用 forced reflow 实现跨页滑动动画
  - 激活链接去除静态背景色，由 slider 统一处理；hover 效果保留
  - nav.css 补充 slider 样式，transitions.js 补充定位逻辑，所有 4 个 HTML 页面添加 slider 元素
- 首页欢迎框添加环境动画：图标浮动（hero-icon-float 3s）、背景光晕脉动（hero-glow-pulse 4s）、渐变色流动（gradient-shift 8s），均支持 `prefers-reduced-motion`
