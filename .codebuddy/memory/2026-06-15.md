## 2026-06-15

- 修复首页核心卡片被挤成一条线的问题：`#homeView` 复用 `.content-wrapper` 的 flex row 布局，导致各 section 横向并排。在 `home.css` 中为 `#homeView` 添加 `flex-direction: column` 覆盖。
- 修复 `config.json` 缺失顶层闭合花括号导致 JSON 解析失败、所有页面配置加载失效的问题。
- 修复 `link.html` 和 `about.html` 引用不存在的 `js/theme.js`，改为内联主题切换逻辑。
- 启动本地 HTTP 服务器于 9090 端口（8080/3000 已有其他服务占用）。
